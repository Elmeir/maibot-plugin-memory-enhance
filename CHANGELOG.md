# 更新日志

## 0.4.0（2026-09-22）

- 新增**日记指令**（无权限时静默不回复，本地控制台默认放行）：
  - `/日记 [条数]`：按时间正序列出**当前聊天流**的模型日记（默认 10 条，上限 50），
    用真实日记口吻书写——`2026年9月22日 星期二 下午4点 内容`，同一天后续条目只写时间；
  - `/删日记`：返回**全部**日记列表（跨流、标注来源流，供跨流删除管理）；
  - `/删日记 <编号>`：按编号全局删除日记（可删任意流的日记），删除后回显内容摘要；
- 两条指令均声明 `permission="public"`，由插件内复刻宿主权限判定（与
  `plugin.permission` 操作员名单 / `plugin.command_permissions` 放行规则同源）自行校验，
  无权限时静默拦截（不回复「没有权限」）；manifest 补声明 `send.text` 能力。
- 新增**日记指令管理员 QQ** 配置（`diary.admin_qqs`）：填写的 QQ 号直接放行 /日记、/删日记
  （无需改宿主配置），仅命中 qq 平台。
- 新增**通知 LLM 日记被查看**开关（`diary.notify_context`，默认关）：开启后每次 /日记 会把
  「用户指令原文（用 `message.get_recent` 绕过历史过滤直读最近原始消息）+ 被查看日记的正文
  （纯客观、不带编号、无引导）」作为内部参考消息注入模型上下文，**planner 与 replyer 各注入
  一次**——planner 阶段据此可自行决定调用 `search_memory` 关联回忆，replyer 阶段让回复带上
  「知道自己日记被看过」；只作用于当次请求，不入聊天历史、同流匹配、不影响模型判断。
- 细节：`/日记` 同一天分组渲染（`_render_diary_voice`）改为实例方法；
  补齐本地自检 `self_test.py`（本地数据目录独立建 `.self_test_tmp`），并扩展
  `.gitignore` 忽略自检脚本与临时目录。

## 0.3.0（2026-09-19）

- 配置页新增**「调试」页签**：诊断日志开关（检索范围解析 / 群聊流缓存状态）+ 三张「工具信息」
  卡片（search_memory / query_memory / write_diary 的 LLM 视角定义：可见性 / 描述 / 参数，只读）；
- 配置页改为页签布局（数据源 / 检索 / 工具与日记 / 宿主参数 / 调试）；
- 「宿主检索参数」与工具信息写入配置值展示（修复只读框显示为空的问题；实现与 reply-control 一致）。

## 0.2.0（2026-09-18）

- 项目更名：记忆检索增强 → **记忆增强**（目录与插件 ID：`memory-enhance`）；
- 工具接入方式可切换（配置「工具」页签）：
  - 「新增工具」（默认）：`search_memory` 始终随工具列表提供，planner 钩子自动从列表移除
    内置 `query_memory`——无需任何宿主配置，装上即用；
  - 「替换原生」：`query_memory` 同名接管（需先在宿主关闭内置记忆检索）；
  - 两个检索工具共享实现，均以 `visibility="deferred"` + planner 钩子按配置补回常驻定义
    （standards/host-behavior.md §11/§12 的既有手法）；
- 新增**日记**：`write_diary` 工具——模型随时记录想记住的内容（本插件自有 SQLite，
  独立于宿主记忆库）；检索时作为第四路数据源以 `[日记] 本地时间 内容` 回看；
  日记自动归属写入时的聊天流，检索范围跟随「跨聊天流检索」开关（默认仅当前流，
  与其它数据源的隐私语义一致；旧库自动迁移补列）；
- 数据源页签新增「日记」开关；日记写入返回带 `[日记]` 前缀与记录时间；
- 「跨聊天流检索」由开关改为**三态下拉**：关闭（默认）/ 仅群聊 / 全部——「仅群聊」经
  `chat.get_group_streams` 枚举群聊流逐流检索后合并去重，**私聊记忆在任何模式下都不跨流**；
  manifest 补声明 `chat.get_group_streams` 能力。

## 0.1.0（2026-09-18）

- 首版：增强版 `query_memory` 工具，同名接管宿主内置检索（需先在宿主配置中禁用内置
  `a_memorix.integration.enable_memory_query_tool`，插件加载时自检并提示）；
- 三路数据源（各自独立开关）：段落/关系路（宿主 `knowledge.search`）、事实路
  （只读直读 `metadata.db` 的 `fact_claims`：active 状态 + `valid_to` 过期过滤 +
  person/chat 范围过滤 + LIKE 匹配 + 可信度排序）、经历路（`mode=episode`，默认关）；
- 跨聊天流检索开关（默认关；开启后 `chat_id` 置空 = 全库，与宿主启发式记忆召回同款语义）；
- 只读插件：不写入任何记忆数据，不新增 embedding 调用。
